# I have these setup with Gnome3 desktops but there is some weird environment issue and when starting them they can't find java in the path
export PATH=/opt/eclipse:/opt/giteye:$PATH
export PATH=$PATH:/Applications/Postgres.app/Contents/Versions/latest/bin
